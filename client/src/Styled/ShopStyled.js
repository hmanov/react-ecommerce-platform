import styled from 'styled-components';

export const ShopTitle = styled.h2`
  text-align: center;
  color: ${(props) => props.theme.primary};
  font-size: 2em;
  letter-spacing: 2px;
  margin-top: 60px;
  margin-bottom: 60px;
`;
